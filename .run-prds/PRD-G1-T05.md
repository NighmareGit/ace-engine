# PRD: fuzz-escalation-rejected v1 — document + test the rejection boundary

## Task G1-T05: fuzz-escalation-rejected v1 — document + test the rejection boundary

Document that fuzzy escalation is rejected for v1 (same format-drift class as fence mangling). Test that the validator refuses fuzzy-match paths. Reference Aider-style escalation as the documented upgrade path. No new production code.
